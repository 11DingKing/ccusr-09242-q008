# 招商项目状态机与统计来源

> 本文档基于当前代码实现整理，用于后续修状态流转时对照参考。

---

## 一、五大模块关联关系

### 1.1 实体关系图（ER 概览）

```
┌─────────────────┐       ┌──────────────────┐
│  Entity（主体）  │1─────*│  Project（项目）  │
│  - initiator_id │       │  - park_id        │
└─────────────────┘       └────────┬─────────┘
                                   │
                                   │1
                                   *│
                    ┌──────────────┴──────────────┐
                    │                             │
                    │1                            │1
                    *                             *
        ┌────────────────────┐        ┌────────────────────┐
        │ CooperationIntent  │        │ ProjectMilestone   │
        │  （合作意向）       │        │  （里程碑）         │
        └─────────┬──────────┘        └────────────────────┘
                  │1
                  *
        ┌────────────────────┐
        │ NegotiationRecord  │
        │  （洽谈记录）       │
        └────────────────────┘
```

### 1.2 详细关联说明

| 关联方向 | 字段 | 说明 | 代码位置 |
|---------|------|------|---------|
| Project → Entity | `Project.initiator_id` → `Entity.id` | 每个项目有一个发起主体（招商方），一个主体可发起多个项目 | [models.py#L102-L178 |
| Project → IndustrialPark | `Project.park_id` → `IndustrialPark.id` | 每个项目落地一个园区，一个园区容纳多个项目 | [models.py#L80-L99 |
| Project → CooperationIntent | `CooperationIntent.project_id` | 一个项目可收到多条合作意向 | [models.py#L193-L236 |
| CooperationIntent → Entity（提交方）| `submitter_id` | 意向的提交主体 | [models.py#L198 |
| CooperationIntent → Entity（对方） | `counterparty_id` | 意向的合作对方（可选） | [models.py#L199 |
| CooperationIntent → NegotiationRecord | `intent_id` | **洽谈记录挂在意向下，不直接挂项目 | [models.py#L238-L257 |
| Project → ProjectApproval | 一对一 | 立项信息，项目立项后生成 | [models.py#L260-L283 |
| Project → ProjectMilestone | `project_id` | 一个项目有多条里程碑，按 sequence 排序 | [models.py#L286-L309 |
| Project → ProjectStatusLog | `project_id` | 状态变更日志 | [models.py#L312-L324 |
| Project → MonthlyCapacityReport | `project_id` | 投产项目的月度产能报告 | [models.py#L327-L343 |
| Project → CapacityFollowUp | `project_id` | 产能未达标时自动/手动跟进事项 | [models.py#L346-L364 |
| Project → ProjectCategory | `project_id` | 项目加工品类标签 | [models.py#L181-L190 |

---

## 二、项目状态枚举

定义位置：[enums.py#L24-L30

| 枚举值 | 含义 |
|--------|------|
| `ATTRACTING_INVESTMENT` | 招商中 |
| `NEGOTIATING` | 洽谈中 |
| `ESTABLISHED` | 已立项 |
| `UNDER_CONSTRUCTION` | 建设中 |
| `COMMISSIONED` | 已投产 |

---

## 三、项目状态机：状态被改动的所有入口

### 3.1 状态流转规则

定义位置：[crud.py#L17-L68

**状态顺序**：`招商中 → 洽谈中 → 已立项 → 建设中 → 已投产

**允许的正向流转（无需原因）：
- 招商中 → 洽谈中
- 洽谈中 → 已立项
- 已立项 → 建设中
- 建设中 → 已投产

**允许的逆向流转（必须填写 `reason`）：
- 洽谈中 → 招商中
- 已立项 → 洽谈中
- 建设中 → 已立项
- 已投产 → 建设中

**禁止跨级跳转（无论正向或逆向超过一步）

---

### 3.2 入口 1：创建项目（默认初始化）

位置：[models.py#L108-L113

创建时 `status` 默认值为 `ProjectStatus.ATTRACTING_INVESTMENT`（招商中）。

不记录状态日志。

---

### 3.3 入口 2：提交合作意向（自动推进）

位置：[crud.py#L340-L360

触发 API：`POST /workflow/intents`（[workflow.py#L12-L29）

**触发条件**：
- 提交合作意向时，若项目状态为 `ATTRACTING_INVESTMENT

**状态变更**：
- `ATTRACTING_INVESTMENT` → `NEGOTIATING`

**日志记录**：
```python
reason = "收到合作意向，转入洽谈阶段"
```

**额外联动**：
- 路由层还做了前置校验：只有「招商中」的项目才能提交合作意向（[workflow.py#L17-L21）

---

### 3.4 入口 3：立项操作（自动推进）

位置：[crud.py#L405-L463

触发 API：`POST /workflow/projects/{project_id}/approve`（[workflow.py#L123-L146）

**触发条件**：
- 项目必须是 `NEGOTIATING` 状态
- 项目尚未立项（`project.approval` 不存在）

**状态变更**：
- `NEGOTIATING` → `ESTABLISHED`

**日志记录**：
```python
reason = f"正式立项，文号：{approval_number}"
```

**额外操作**：
- 创建 `ProjectApproval` 立项记录
- 生成里程碑：若 `approval_in.milestones` 为空则自动生成 5 条默认里程碑（奠基→主体→设备→试产→投产），基于 approval_date 推算日期

**路由前置校验**：
- 只有「洽谈中」的项目才能立项（[workflow.py#L138-L142）
- 不可重复立项（[workflow.py#L136-L137）

---

### 3.5 入口 4：更新里程碑（自动推进两个节点）

位置：[crud.py#L499-L580]

触发 API：`PUT /workflow/milestones/{milestone_id}`（[workflow.py#L200-L216]

#### 节点 A：任意里程碑进入「进行中」
**触发条件**：
- 更新里程碑状态为 `IN_PROGRESS` 或 `COMPLETED` 时，只要存在至少一个里程碑状态为 `IN_PROGRESS`
- 且项目当前状态为 `ESTABLISHED`

**状态变更**：
- `ESTABLISHED` → `UNDER_CONSTRUCTION`

**日志记录**：
```python
reason = "里程碑推进中，项目进入建设期"
```

#### 节点 B：最后一个里程碑（正式投产）完成
**触发条件**：
- 里程碑列表中 `sequence` 最大的那条
- `milestone_type == MilestoneType.OFFICIAL_PRODUCTION`
- 状态为 `COMPLETED`
- 项目当前状态为 `UNDER_CONSTRUCTION`

**状态变更**：
- `UNDER_CONSTRUCTION` → `COMMISSIONED`

**日志记录**：
```python
reason = "正式投产里程碑完成，项目已投产"
```

**里程碑自身的流转规则**（独立于项目状态机）：
- 不允许退回到 `NOT_STARTED`
- `COMPLETED` 之前必须先经过 `IN_PROGRESS`
- `IN_PROGRESS` 只能从 `NOT_STARTED` 或 `DELAYED` 转来
- 前置里程碑（sequence 更小的）必须全部 `COMPLETED` 才能推进当前里程碑

---

### 3.6 入口 5：手动状态流转接口

位置：[crud.py#L271-L296

触发 API：`POST /projects/{project_id}/status`（[projects.py#L98-L117）

调用 `change_project_status()`，执行完整校验 `validate_status_transition()` 严格校验，记录操作人、原因、备注，写入 `ProjectStatusLog`。

可做所有合法的正向/逆向流转。

---

### 3.7 状态流转总览图

```
  创建项目
      │
      ▼
┌─────────────┐   提交合作意向      ┌─────────────┐
│  招商中   │ ───────────────▶ │  洽谈中    │
│ ATTRACTING│                 │ NEGOTIATING│
│ _INVESTMEN│ ◀────────────── │            │
└─────────────┘   手动退回(需原因 └──────┬──────┘
                                      │
                                      │ 立项操作
                                      ▼
                               ┌─────────────┐
                               │  已立项     │
                               │ ESTABLISHED│
                               │             │
                               └──────┬──────┘
                                      │
                                      │ 里程碑进入进行中
                                      ▼
                               ┌──────────────┐
                               │  建设中      │
                               │ UNDER_CONS. │
                               │ TRUCTION    │
                               └──────┬───────┘
                                      │
                                      │ 正式投产里程碑完成
                                      ▼
                               ┌──────────────┐
                               │  已投产     │
                               │ COMMISSIONED│
                               └──────────────┘

  注：任何相邻状态之间均可手动退回（需填写原因）
```

---

## 四、里程碑完成后对统计的影响

**重要：统计接口全部为「实时聚合查询」，**没有独立的统计表或缓存表。每次请求统计接口时，从各函数遍历 `get_overall_statistics()` 或 `get_capacity_overview_statistics()` 实时计算。

因此里程碑完成 → 改变项目状态 → 下次查询统计时数值变化。

### 4.1 里程碑 → 项目状态 → 影响的统计项

| 里程碑事件 | 项目状态变化 | 影响到的统计字段（get_overall_statistics） |
|----------|------------|----------------------------------|
| 首个里程碑标记为进行中 | ESTABLISHED → UNDER_CONSTRUCTION | `under_construction_count` +1<br>`established_count` -1<br>园区维度：`under_construction_projects` +1, `established_projects` -1 |
| 正式投产里程碑完成 | UNDER_CONSTRUCTION → COMMISSIONED | `commissioned_count` +1<br>`under_construction_count` -1<br>`commission_rate` 重算<br>园区维度同上变化 |

**正式投产完成**还会影响 **产能统计接口**（`get_capacity_overview_statistics`）：
- `total_commissioned_projects` +1
- 该项目进入产能统计的计算范围（承诺产能、达产率、本地采购额）

---

## 五、园区统计各指标数据来源

### 5.1 全局统计总览（get_overall_statistics）

位置：[crud.py#L583-L670]

| 统计字段 | 数据来源 | 计算方式 |
|-----------|----------|---------|
| `total_projects` | `Project` 表 | COUNT(*) |
| `attracting_investment_count` | `Project.status` | WHERE status = ATTRACTING_INVESTMENT |
| `negotiating_count` | `Project.status` | WHERE status = NEGOTIATING |
| `established_count` | `Project.status` | WHERE status = ESTABLISHED |
| `under_construction_count` | `Project.status` | WHERE status = UNDER_CONSTRUCTION |
| `commissioned_count` | `Project.status` | WHERE status = COMMISSIONED |
| `total_planned_investment_10k` | `Project.planned_investment_10k` | 所有项目 SUM |
| `total_agreed_investment_10k` | `ProjectApproval.agreed_investment_10k` | 有立项信息的项目 SUM |
| `total_expected_output_value_10k` | `Project.expected_output_value_10k` | 所有项目 SUM |
| `total_expected_jobs` | `Project.expected_jobs` | 所有项目 SUM |
| `commission_rate` | 已投产 / (已立项+建设中+已投产) | 百分比，保留 2 位小数 |
| `parks[*]` | 按 `park_id` 分组后同上规则 | |
| `categories[*]` | 按 `ProjectCategory` 分组 | 项目数 / planned_investment / expected_annual_capacity |

---

### 5.2 园区维度统计（parks 数组内字段）

| 字段 | 来源 |
|------|------|
| `total_projects` | 该园区下项目总数 |
| `established_projects` / `under_construction_projects` / `commissioned_projects` / `attracting_projects` / `negotiating_projects` | 各状态计数 |
| `total_agreed_investment_10k` | 该园区项目的 agreed_investment SUM |
| `total_planned_investment_10k` | 该园区项目的 planned_investment SUM |
| `commission_rate` | commissioned / (established + under_construction + commissioned） |

---

### 5.3 品类维度统计（categories 数组内字段）

| 字段 | 来源 |
|------|------|
| `project_count` | 该品类关联的项目数 |
| `total_investment_10k` | 项目 planned_investment SUM |
| `total_capacity_tonnes` | 项目 expected_annual_capacity SUM |

---

### 5.4 产能兑现统计（get_capacity_overview_statistics）

位置：[crud.py#L907-L1023

**统计范围**：仅 `Project.status = COMMISSIONED` 的项目。

| 字段 | 数据来源 | 计算方式 |
|------|----------|---------|
| `total_commissioned_projects` | `Project.status = COMMISSIONED | COUNT |
| `total_reporting_projects` | 有至少一条 MonthlyCapacityReport 的项目数 | COUNT |
| `total_promised_capacity_tonnes` | `Project.promised_monthly_capacity_tonnes，若为空则 `expected_annual_capacity_tonnes / 12 | 全部已投产项目 SUM |
| `total_actual_output_tonnes` | 各项目最新一条 MonthlyCapacityReport.actual_output_tonnes（取报告年份月份最大的那条） | SUM |
| `overall_utilization_rate` | total_actual / total_promised | 百分比 |
| `total_local_procurement_10k` | 各项目最新一条报告的 local_material_procurement_10k | SUM |
| 园区维度 parks[*] | 按 park_id 分组同上规则 | |
| 品类维度 categories[*] | 按 ProjectCategory 分组同上规则 | |

---

## 六、字段用途分类：展示用 vs 业务判断用

### 6.1 Project（合作项目）字段

| 字段 | 业务判断用 | 仅展示用 | 说明 |
|------|:----------:|:--------:|------|
| `status` | ✅ | | 状态流转、统计计数、路由层准入校验 |
| `park_id` | ✅ | | 路由层校验、统计分组 |
| `initiator_id` | ✅ | | 列表筛选条件 |
| `planned_investment_10k` | ✅ | | 统计汇总 |
| `expected_annual_capacity_tonnes` | ✅ | | 统计汇总、承诺产能推算（/ 12） |
| `promised_monthly_capacity_tonnes` | ✅ | | 月度产能报告的承诺值对比，达产率计算基准，若有则优先使用 |
| `expected_output_value_10k` | ✅ | | 统计汇总 |
| `expected_jobs` | ✅ | | 统计汇总 |
| `commissioned_date` | | ✅ | 纯展示，未参与任何判断 |
| `construction_cycle_months` | | ✅ | 纯展示 |
| `planned_land_area_mu` | | ✅ | 纯展示 |
| `expected_local_procurement_pct` | | ✅ | 纯展示 |
| `name` / `project_code` | ✅（唯一校验） | | 创建时查重 |
| `investment_direction` | | ✅ | 纯展示 |
| `background` | | ✅ | 纯展示 |
| `market_analysis` | | ✅ | 纯展示 |
| `cooperation_modes` | | ✅ | 纯展示 |
| `support_requirements` | | ✅ | 纯展示 |
| `responsible_department` | | ✅ | 纯展示 |
| `project_leader` | ✅（跟进事项自动带责任人 | |
| `leader_phone` | | ✅ | 纯展示 |
| `publish_date` | | ✅ | 纯展示 |

---

### 6.2 ProjectApproval（立项信息）字段

| 字段 | 业务判断用 | 仅展示用 | 说明 |
|------|:----------:|:--------:|------|
| `agreed_investment_10k` | ✅ | | `total_agreed_investment` 统计来源 |
| `agreed_capacity_tonnes` | | ✅ | 纯展示 |
| `agreed_land_area_mu` | | ✅ | 纯展示 |
| `construction_start_deadline` | | ✅ | 纯展示 |
| `completion_deadline` | | ✅ | 纯展示 |
| `approval_number` / `approval_date` / `approving_authority` | | ✅ | 纯展示，用于日志 reason 文案 |
| `main_content` / `approval_conditions` / `approved_by` | | ✅ | 纯展示 |
| 存在性（project.approval 是否为 None） | ✅ | | 立项重复操作的校验依据 |

---

### 6.3 ProjectMilestone（里程碑）字段

| 字段 | 业务判断用 | 仅展示用 | 说明 |
|------|:----------:|:--------:|------|
| `sequence` | ✅ | | 前置校验（sequence 小的先完成）、判断最后一条 |
| `milestone_type` | ✅ | | 最后一条必须是 OFFICIAL_PRODUCTION 才触发投产 |
| `status` | ✅ | | 项目状态自动推进判断、前置校验 |
| `planned_date` | | ✅ | 纯展示 |
| `actual_date` | | ✅ | 纯展示 |
| `completion_rate` | | ✅ | 纯展示 |
| `name` / `description` / `responsible_person` / `remarks` | | ✅ | 纯展示 |

---

### 6.4 CooperationIntent（合作意向）字段

| 字段 | 业务判断用 | 仅展示用 | 说明 |
|------|:----------:|:--------:|------|
| `status` | ✅ | | 意向自身状态流转（SUBMITTED→IN_DISCUSSION 等 |
| `project_id` | ✅ | | 路由层项目存在性校验、列表筛选 |
| `submitter_id` / `counterparty_id` | ✅ | | 路由层主体存在性校验、列表筛选 |
| `reviewed_at` | ✅ | | 意向状态变更时自动更新（首次进入评审/洽谈时设置 |
| `cooperation_mode` / `proposed_investment_10k` / `proposed_capacity_tonnes` / `cooperation_content` / `expected_timeline` / `requirements` / `submitter_comments` / `reviewer` / `review_comments` | | ✅ | 纯展示 |

洽谈记录（NegotiationRecord）所有字段均为**纯展示**，仅 `create_negotiation` 会触发意向状态 SUBMITTED/REVIEWING → IN_DISCUSSION（[crud.py#L381-L393]）。

---

### 6.5 MonthlyCapacityReport（月度产能）字段

| 字段 | 业务判断用 | 仅展示用 | 说明 |
|------|:----------:|:--------:|------|
| `project_id` | ✅ | | 项目必须 COMMISSIONED 才能创建，查重（项目+年月唯一） |
| `report_year` + `report_month` | ✅ | | 最新报告的判定依据（取年月最大的） |
| `actual_output_tonnes` | ✅ | | 产能统计 actual_output、缺口计算、自动生成 CapacityFollowUp |
| `capacity_utilization_rate` | ✅（自动推算） | | 若未填则自动 = actual/promised |
| `local_material_procurement_10k` | ✅ | | 产能统计本地采购额汇总 |
| `employee_count` | | ✅ | 纯展示 |
| `remarks` / `reported_by` | | ✅ | 纯展示 |

---

### 6.6 CapacityFollowUp（产能跟进）字段

| 字段 | 业务判断用 | 仅展示用 | 说明 |
|------|:----------:|:--------:|------|
| `status` | ✅ | | 列表筛选条件；变为已解决/已关闭时自动取消未结提醒 |
| `priority` | ✅ | | 列表筛选、自动根据缺口率推算（gap_pct 自动推算：≥50紧急，≥30高，≥15中，其余低） |
| `gap_percentage` | ✅（推算 priority） | | 自动生成跟进时填入；提醒编排时作为无最新报告情况下的缺口回退值 |
| `project_id` | ✅ | | 列表筛选、存在性校验 |
| `last_contact_at` | ✅ | | 上次联系时间，提醒编排计算下一次提醒的基准；处理提醒时自动回填，也可手工补录 |
| `report_id` | | ✅ | 关联报告，展示用 |
| `title` / `description` / `responsible_person` / `deadline` / `resolution` | | ✅ | 纯展示（`responsible_person` 会随提醒转交同步更新） |

---

### 6.7 Entity（主体档案）字段

| 字段 | 业务判断用 | 仅展示用 | 说明 |
|------|:----------:|:--------:|------|
| `name` | ✅ | | 唯一校验、列表展示关联名称 |
| `region` | ✅ | | 列表筛选（东盟方/广西方） |
| `id` | ✅ | | 作为项目 initiator_id / intent 双向外键 |
| 其余字段（`country_or_province` / `city` / `contact_person` / `registered_capital` 等） | | ✅ | 纯展示 |

EntityCapability（主体产能）字段均为**纯展示**，不参与业务判断。

---

## 七、关键业务联动速查表

| 操作 | 项目状态影响 | 是否写状态日志 | 影响哪些统计 | 代码位置 |
|------|-------------|--------------|------------|---------|
| 创建项目 | → 招商中（默认） | 否 | total_projects +1 | [crud.py#L236-L247 |
| 提交合作意向 | 招商中→洽谈中 | 是 | negotiating_count +1 / attracting -1 | [crud.py#L348-L357]
| 立项 | 洽谈中→已立项 | 是 | established +1 / negotiating -1 | [crud.py#L450-L460]
| 标记里程碑进行中 | 已立项→建设中（首个） | 是 | under_construction +1 / established -1 | [crud.py#L566-L578]
| 完成正式投产里程碑 | 建设中→已投产 | 是 | commissioned +1 / under_construction -1，产能统计纳入范围 | [crud.py#L547-L564]
| 手动状态流转 | 任意合法流转 | 是 | 对应状态计数 | [crud.py#L271-L296]
| 登记月度产能 | 无（仅 COMMISSIONED 可登） | 否 | 产能 actual_output / 达产率 / 本地采购额 | [crud.py#L723-L776]

---

## 八、产能跟进提醒编排

实现位置：`app/services/reminders.py`（核心逻辑）、`app/routers/reminders.py`（接口）、`app/models.py` 的 `FollowUpReminder` / `FollowUpReminderEvent` 两表。

### 8.1 提醒生成规则

- **间隔分档**（与跟进事项 priority 档位一致）：缺口 ≥50% 每 3 天、≥30% 每 7 天、≥15% 每 14 天、其余每 30 天。
- **缺口来源**：优先取项目最新月度报告的达产率折算（100 − 达产率）；无报告时回退到跟进事项登记的 `gap_percentage`；都没有则按 30 天。
- **到期时间**：`上次联系时间 + 间隔天数`；从未联系过（`last_contact_at` 为空）则立即进入提醒窗口。
- **节假日顺延**：由调用方在生成时提供 `holidays`（日期列表）与 `defer_weekends`（是否跳过周末），按请求时区的**当地日期**判断，顺延规则存入生成依据（`basis`），处理完成续排下一次时沿用。
- **生成依据**：每次生成把承诺产能、最新报告、达产率、缺口、间隔、上次联系时间、顺延前后时间、节假日规则快照存入 `FollowUpReminder.basis`，可通过 `GET /capacity/reminders/{id}/basis` 查询。

### 8.2 提醒状态机

```
待提醒(PENDING) ──到期扫描──▶ 待处理(DUE) ──领取──▶ 已领取(CLAIMED) ──处理──▶ 已处理(DONE)
     │                          │                                            ▲
     └──────── 延期(原因必填) ──▶ 已延期(DEFERRED) ──到期扫描──▶ 待处理 ──────┘
     任意开放状态 ──跟进事项关闭/解决──▶ 已取消(CANCELLED)
```

- 开放状态：待提醒 / 待处理 / 已领取 / 已延期；终态：已处理 / 已取消。
- 处理完成（complete）时回填跟进事项 `last_contact_at`，并按最新达产数据自动编排下一次提醒。
- 服务启动（lifespan）自动执行到期扫描，重启后继续处理到期事项；`POST /capacity/reminders/process-due` 可手动补跑，扫描幂等。

### 8.3 去重与并发设计

| 机制 | 说明 |
|------|------|
| 部分唯一索引 | `uq_follow_up_reminders_open`：`follow_up_id` 上 `WHERE is_open = 1` 唯一，保证每个跟进事项至多一条未结提醒 |
| 幂等生成 | 已有未结提醒的事项跳过（`skipped_existing`）；并发撞唯一索引时按已存在处理 |
| CAS 乐观锁 | 领取/延期/转交/处理均以 `id + version + is_open` 为更新条件，冲突返回 409；转交可显式传 `expected_version` |
| 读写分段 | 写操作前先释放读事务，写事务首条语句即为写操作，避免 SQLite 读锁升级冲突 |
| 处理链 | `FollowUpReminderEvent` 记录生成/到期/领取/延期/转交/处理/取消，转交不覆盖历史，链上可见历任责任人 |

### 8.4 接口一览（前缀 `/api/v1/capacity/reminders`）

| 接口 | 说明 |
|------|------|
| `POST /generate` | 编排生成提醒（幂等去重；`timezone`/`holidays`/`defer_weekends`/`as_of` 由调用方提供） |
| `POST /process-due` | 扫描到期提醒（启动时自动执行） |
| `GET /` | 工作清单：按责任人/状态/跟进事项/项目/是否到期过滤 |
| `GET /{id}` / `GET /{id}/basis` / `GET /{id}/events` | 详情 / 生成依据 / 处理链 |
| `POST /claim` | 按责任人批量领取（仅本人或未分配可领） |
| `POST /defer` | 批量延期（`defer_days` 或 `defer_until` 二选一，原因必填） |
| `POST /{id}/transfer` | 转交责任人（保留处理链，同步跟进事项责任人） |
| `POST /{id}/complete` | 处理完成，自动编排下一次提醒 |
